# Session Snapshot: 2026-03-02-21-30
Trigger: auto

## Git State
```
Branch: main

ec643d3 fix: manual readline redraw when _refreshLine unavailable in Bun
86a5947 fix: invert picker arrow key direction to match visual layout
4ea1188 fix: replace _ttyWrite override with prependListener for Bun compat
7ae1460 feat: tab cycles picker selection, enter accepts current item
ccbbeca fix: reset cursor to column 1 before each picker line render
328ed6b fix: render file picker above prompt with relative cursor movement
9e4f144 fix: prevent output from disrupting readline prompt
3ac0484 feat: fix stale session hint and add interactive @ file picker
4626214 feat: add analyze-plan-act protocol, progressive knowledge, and reflection loop
c5694b1 refactor: remove hardcoded ISA knowledge, make optimization harness domain-agnostic
675ffbf feat: add golden solution injection and ISA verifier agent
96b9eb0 feat: add micro phase, top-2 seeding, self-check, and final push retry
59e0654 feat: add path diversity, success/failure patterns, and few-shot examples
9a7b426 feat: add phase-gated pipeline with deep study and adaptive model selection
08d5b8f fix: pass workdir and test config to researcher stall context
```

## Working Tree
```
 m original_performance_takehome
Am perf-challenge
?? compact/session_2026-03-02-21-30.md
```

## Recent Conversation Context
```
에서 readline이 바꾼 상태를 되돌리고 피커 동작 적용
4. completer가 피커 활성 시 빈 결과 반환 (Tab에서 readline 개입 방지)
이제 `_ttyWrite` 오버라이드 전체를 제거하고, `prependListener` + `setImmediate` 방식으로 교체.
이제 `setImmediate` 안의 피커 로직에서 `_pickerAction`을 처리하도록 수정.
완료. 변경 요약:

**문제**: `_ttyWrite`가 Bun readline에 존재하지 않아 Tab/Up/Down/Escape 모두 동작 안 함

**수정 방식**: `_ttyWrite` 오버라이드 전부 제거하고 `prependListener` 패턴으로 교체

- `process.stdin.prependListener("keypress")` — readline **이전에** 실행되어 현재 `rl.line`/`rl.cursor` 저장 + 키 종류 플래그 설정
- `setImmediate`에서 readline이 이미 처리한 상태를 **되돌리고** 피커 동작 적용:
  - **Tab** → 저장된 라인 복원 → 선택된 파일 경로로 `@query` 교체 → `_refreshLine` → 피커 닫기
  - **Up/Down** → 저장된 라인 복원 (readline 히스토리 무효화) → selection 이동 → 리렌더
  - **Escape** → 피커 닫기
- completer가 피커 활성 시 빈 배열 반환 (readline이 Tab에서 개입 못 하도록)
피커가 역순 렌더링(selected=0이 하단)이라 delta가 반대입니다.
렌더링이 역순(`results[N-1]`이 화면 위, `results[0]`이 프롬프트 바로 위)이므로 delta를 반대로.
피커가 역순 렌더링(selected=0이 프롬프트 바로 위, 높은 인덱스가 화면 위쪽)이라 Up → `+1` (화면 위로), Down → `-1` (화면 아래로)로 수정했습니다.
`_refreshLine`도 Bun에 없어서 시각적 갱신이 안 되는 겁니다. 백스페이스 누르면 readline이 자체 리드로우를 해서 그때야 보이는 거고요.
`_refreshLine`이 Bun에 없으니 수동으로 프롬프트+라인을 다시 그리는 헬퍼를 만들겠습니다.
이제 `_refreshLine?.()` 호출을 모두 `refreshLine()`으로 교체.
`file-picker.ts`의 `clearRender`에서도 `_refreshLine`을 호출하고 있습니다. 피커도 `refreshLine`이 필요.
피커에서 `rl` 대신 `refreshLine` 콜백을 받도록 변경.
repl.ts에서 `FilePicker` 생성자 호출도 변경.
`refreshLine`은 `filePicker` 생성 이후에 정의되어 있으므로, 콜백으로 감싸서 전달.
`currentPrompt`도 plan 모드일 때 갱신되어야 합니다. `rl.question` 호출 부분 확인.
수정 완료. Bun readline에 `_refreshLine`이 없어서 Tab 시 내부 상태는 바뀌지만 화면이 안 그려졌던 문제입니다.

`refreshLine()` 헬퍼 추가:
- `_refreshLine` 있으면 사용 (Node.js 호환)
- 없으면 `\r\x1b[2K` + 프롬프트 + 라인 내용 수동 출력 + 커서 위치 복원
현재 에이전트 실행 중 ESC 처리와 취소 메커니즘을 확인하겠습니다.
현재 Ctrl+C만 에이전트 취소를 하고 있습니다. ESC에도 같은 동작을 추가하면 됩니다. 에이전트 모드(layout keypress) 쪽도 확인.
```

## Project Files (recently modified)
```
/Users/aaron-son/Documents/orchestrator/src/repl/repl.ts
/Users/aaron-son/Documents/orchestrator/src/repl/file-picker.ts
/Users/aaron-son/Documents/orchestrator/src/repl/renderer.ts
/Users/aaron-son/Documents/orchestrator/src/repl/file-ref.ts
/Users/aaron-son/Documents/orchestrator/src/repl/commands.ts
/Users/aaron-son/Documents/orchestrator/src/db/store.ts
/Users/aaron-son/Documents/orchestrator/src/core/optimization-harness.ts
/Users/aaron-son/Documents/orchestrator/src/core/researcher.ts
/Users/aaron-son/Documents/orchestrator/src/core/brainstorm.ts
/Users/aaron-son/Documents/orchestrator/src/repl/layout-manager.ts
/Users/aaron-son/Documents/orchestrator/src/core/harness-enforcer.ts
/Users/aaron-son/Documents/orchestrator/src/repl/streamer.ts
/Users/aaron-son/Documents/orchestrator/src/core/orchestrator.ts
/Users/aaron-son/Documents/orchestrator/src/config/types.ts
/Users/aaron-son/Documents/orchestrator/src/core/doom-loop.ts
/Users/aaron-son/Documents/orchestrator/src/agents/dynamic-harness.ts
/Users/aaron-son/Documents/orchestrator/src/benchmark/runner.ts
/Users/aaron-son/Documents/orchestrator/src/sandbox/tree-sitter-parser.ts
/Users/aaron-son/Documents/orchestrator/src/core/events.ts
/Users/aaron-son/Documents/orchestrator/src/benchmark/report-generator.ts
```
